<%=provide(:title, "flash") %>
<div class="container">
  <div class="jumbotron">
    <h1>Welcome to flash!</h1>
    <p class="center">flash is a flashcards rails app</p>
  </div>

<div class="center">
  <% if !current_user.nil? %>
    <%= link_to 'home', user_home_path, class: 'btn btn-default btn-lg' %>
    <% else %>
    <%= link_to 'signup', signup_path, class: 'btn btn-default btn-lg' %>
    <% end %>
  </div>
</div>
  </div>
</div>
